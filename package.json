{
  "name": "greenlyscraping",
  "version": "1.0.0",
  "description": "Greenly Scraping",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "client": "cd client && npm run start",
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install --force",
    "heroku-postbuild": "npm run install-client && npm run build"
  },
  "engines": {
    "node": "16.x"
  },
  "type": "module",
  "author": "yourname",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "compression": "^1.7.4",
    "express": "^4.18.2",
    "install": "0.13.0",
    "linkedin-private-api": "^1.1.2",
    "node-fetch": "^2.6.9",
    "puppeteer": "^19.7.5",
    "socket.io": "^4.5.4"
  }
}
